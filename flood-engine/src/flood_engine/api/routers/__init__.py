"""FastAPI routers, one module per SDS Section 8 resource group (scenarios, runs, exposure).

Empty as of Step 15: every SDS Section 8 endpoint needs persistence
(Step 17) or the async job queue the SDS's own text says that contract
requires (Step 16). See ``flood_engine.api``'s module docstring.
"""
