"""Unit tests: exercise pure, I/O-free logic in isolation.

Primarily flood_engine.core (see tests/unit/core/), plus other modules
that don't require a database, filesystem, or running service to test
(e.g. flood_engine.config, which only reads environment variables).
"""
